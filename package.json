{
  "name": "zk-api-credits",
  "version": "0.0.1",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/api-server",
      "packages/circuits",
      "packages/contracts"
    ]
  },
  "scripts": {
    "postinstall": "husky install",
    "lint": "echo 'no global lint'",
    "format": "echo 'no global format'"
  },
  "devDependencies": {
    "husky": "~9.1.6"
  },
  "packageManager": "yarn@3.2.3",
  "engines": {
    "node": ">=20.18.3"
  }
}
